const path = require('path');

// Convenience script for docs/tooling.
// Prints the schema path so you can quickly locate it.
// eslint-disable-next-line no-console
console.log(path.resolve(__dirname, '..', '..', 'schema.sql'));

